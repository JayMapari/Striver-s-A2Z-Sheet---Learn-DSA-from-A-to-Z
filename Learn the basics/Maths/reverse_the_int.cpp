#include <bits/stdc++.h>
using namespace std;

int main(){
    // int n = -23901;
    // int m = n;
    // if(0>n){
    //     m = m * -1;
    // }
    // string s = to_string(m);
    // int len = s.length();
    // int a[len];
    
    // for(int i = 0;i < len;i++){
    //     a[i] = s[i] - '0';
    // }

    
    // int ans = 0;
    // for(int i = len-1;i >= 0;i--){
    //     ans = ans*10 + a[i];
    // }
    // if(0>n){
    //     ans = ans * -1;
    // }
    // cout << ans;

    
    // int arr[] = {1,2,3,4,0};
    // int len = sizeof(arr)/sizeof(arr[0]);
    int ans = 0;
    // for(int i = len-1;i >= 0;i--){
    //     ans = ans*10 + arr[i];
    // }
    // cout <<ans;
    // if(ans < pow(-2,31) && (pow(2,31)-1)){

    // }
    
}
